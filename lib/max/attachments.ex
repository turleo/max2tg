defmodule Max2tg.Max.Attachments do
  def handle_attachment("PHOTO", payload) do
  end
  def handle_attachment("VIDEO", payload) do
  end
  def handle_attachment("FILE", payload) do
  end
  def handle_attachment("STICKER", payload) do
  end
end
